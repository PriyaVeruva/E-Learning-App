const studentAccount = {}
const studentProfile = (state = studentAccount, action) => {
    switch (action.type) {
     
        default: {
            return {...state }
        }
    }
}
export default studentProfile